[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[user]
	name = Sebastianokunkelo
	email = sebastian.kunkel@poczta.fm
[init]
	defaultBranch = main
